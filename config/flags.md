# Decline rules & flags

This file is inserted verbatim into the underwriting prompt. Edit it freely —
plain English works. The analyzer re-reads it on every deal, so changes take
effect immediately without restarting anything.

## Decline rules

<!-- PLACEHOLDER — replace with your real decline rules. Examples of the kind
     of rules that go here:
- Decline "revenue below minimum" if average true revenue is under $XX,000/mo
- Decline "too many positions" if the merchant already has N or more active advances
- Decline "restricted industry" for: trucking, law firms, ...
- Decline "excessive negative days" if more than N negative days in any month
-->
- (no decline rules configured yet — use standard MCA judgment)

## Flags

<!-- PLACEHOLDER — replace with the specific flags your team uses.
     Anything listed here gets called out on the Suggestion line. -->
- (no custom flags configured yet — flag anything a careful underwriter would)
